var largeBuffer=1
const fs=require('fs');
const { versions } = require('process');
for (i='a'; i<=100000; i++) {
  
  
}

const content= str;
try{
    fs.writeFileSync('./somefile.txt',content);
} catch(err){
    console.error(err);
}